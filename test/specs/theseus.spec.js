import theseus from './index';

describe('theseus', function() {

    // TODO: replace dummy test once `import' actually working
    it('should be an object', function() {
        theseus.should.be.an('object');
    });

});
